{
  "routes": [
    {
      "method": "GET",
      "path": "/venta-efectivos",
      "handler": "venta-efectivo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/venta-efectivos/count",
      "handler": "venta-efectivo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/venta-efectivos/:id",
      "handler": "venta-efectivo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/venta-efectivos",
      "handler": "venta-efectivo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/venta-efectivos/:id",
      "handler": "venta-efectivo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/venta-efectivos/:id",
      "handler": "venta-efectivo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
